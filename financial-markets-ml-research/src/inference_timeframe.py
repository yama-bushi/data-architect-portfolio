"""
Runs model inference on new data to generate signals/labels.
"""

# import joblib
import pandas as pd

def run_inference(X):
    # model = joblib.load('model_30m.pkl')
    # preds = model.predict(X)
    # return preds
    pass

if __name__ == "__main__":
    # X_new = pd.read_csv('X_new_30m.csv')
    # predictions = run_inference(X_new)
    pass
